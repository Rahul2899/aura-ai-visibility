services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: peec
      POSTGRES_PASSWORD: peec
      POSTGRES_DB: peec
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
